apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: tito-pizzeria-stack
  namespace: argocd
spec:
  project: default
  source:
    repoURL: 'https://github.com/rodriguez-22/backend-mc-pnpm.git'
    targetRevision: master
    path: deploy/kubernetes/tito-pizzeria
    helm:
      valueFiles:
        - values.yaml
  destination:
    server: 'https://kubernetes.default.svc'
    namespace: tito-pizzeria
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true