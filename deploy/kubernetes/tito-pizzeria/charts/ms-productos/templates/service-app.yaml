{{- if .Values.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: tito-ms-productos-service
  namespace: {{ .Release.Namespace }}
spec:
  type: ClusterIP
  selector:
    app: tito-ms-productos
  ports:
    - name: http
      protocol: TCP
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.app.containerPort }}
{{- end }}